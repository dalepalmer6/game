package gamestate.cutscene;

import menu.MenuItem;
import system.SystemState;

public class CutsceneMenuItem extends MenuItem {

	/*Doesnt do anything, just a stub so that the cutscene menu can exist*/
	public CutsceneMenuItem(SystemState state) {
		super("",0,0,0,0,state);
	}
}
